# mewyork

Static site for mewyork.xyz.

Deploys happen automatically on push to `main` (GitHub Pages, branch `main`, root).

Design assets come from a separate pipeline; `index.html` is a placeholder holding page until they land.
